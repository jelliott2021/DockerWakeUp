## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Checklist

- [ ] `npm run test:all` passes locally (lint, types, shellcheck, unit/integration with coverage, API, end-to-end, setup script)
- [ ] New behaviour has tests (coverage thresholds in `jest.config.js` must still pass)
- [ ] Docs updated where behaviour or configuration changed (`README.md`, `CONFIGURATION.md`, `CHANGELOG.md`)
